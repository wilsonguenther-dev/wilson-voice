<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.device.audio-input</key>
	<true/>
	<key>com.apple.security.app-sandbox</key>
	<false/>
	<!-- Hardened runtime helpers for Tauri + cpal. NOTE: the two dylib-injection
	     entitlements (library-validation off + dyld env vars) are deliberately
	     NOT granted — they would let DYLD_INSERT_LIBRARIES load an attacker dylib
	     into this non-sandboxed, Accessibility-trusted, keystroke-synthesizing
	     process (a TCC-inheritance escalation). MLX/JIT runs in the separate
	     Python child (its own signature); setting env vars for that child via
	     Command.env() does not need the dyld entitlement. -->
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
</dict>
</plist>
